name: Testnet harvest vault 3

on:
  schedule:
    - cron: '20 06,12,18,00 * * *' # on minute 20 every 6 hours from hour 06
  workflow_dispatch:

jobs:
  test-cron-job:
    runs-on: ubuntu-latest

    steps:
      - name: Invoke testnet harvest on vault 3
        env:
          APP_SECRET: ${{ secrets.APP_SECRET }}
          APP_BASE_URL: ${{ secrets.BASE_URL }}
        run: curl -if $APP_BASE_URL/harvest/97/0x5F08Ed30E3f349bA963B4fBeBfEB757E2ecd094E -H "Authorization:$APP_SECRET"
